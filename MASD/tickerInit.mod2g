use tickerKnowledge as knowledge.
use tickerBeliefs as beliefs.

module tickerInit {
	if 
		true 
	then 
		insert(no_timer)
		+ insert(not_initialized)
		.
}

 % + insert(set_position(scp1, kitchen)) + insert(set_position(scp2, hallway)).