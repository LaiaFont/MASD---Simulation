use  common as knowledge.
use printText as actionspec.

focus=select.
exit=nogoals.

module adultKeepCalm {
	if
		bel(tick, my_name(N), concat_strings(N, "Quietly ", "reading!!!", M))
	then
		delete(tick)
		+ printText(M).
}