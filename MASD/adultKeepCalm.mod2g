use  common as knowledge.
use printText as actionspec.

focus=select.
exit=nogoals.

module adultKeepCalm {
	if
		bel(tick)
	then
		delete(tick)
		+ printText("Quietly reading"). 
		%%+insert(keep_calm).
}