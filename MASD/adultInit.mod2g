use common as knowledge.
use personGoals as goals.
use mapState as beliefs.

module adultInit {
	if 
		true
	then
		subscribe(tickChannel)
		+ subscribe(emergencyChannel)
		+ subscribe(locationChannel)
		. 
}