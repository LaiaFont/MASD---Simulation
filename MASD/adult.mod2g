use common as knowledge.
use printText as actionspec.
use adultKeepCalm as module.
use adultScape as module.

exit=nogoals.

module adult {

	if 
		goal(scape), bel(my_status(S))
	then
		delete(my_status(S)) +
		insert(my_status(scaping)) +
		adultScape.
				
	if
		goal(keep_calm) %, bel(tick)
	then
		adultKeepCalm.
		%delete(tick)
		%+ printText("Quietly reading"). 
		%%+insert(keep_calm).
				
}
